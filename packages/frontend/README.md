# curatering_mobile (Flutter)

Mobile app for Curatering. Run from this directory:

```bash
flutter pub get
flutter run
```

See the [repository root README](../../README.md) for the full monorepo layout.
